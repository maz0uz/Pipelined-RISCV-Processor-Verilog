# Pipelined-RISCV-Processor-Verilog
An implementation of the full RV32I instruction set using verilog. All components were implemented as separate modules. A random test generator was also developed in C++ to create test programs for the processor.
